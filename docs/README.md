## How to build the documentation

First, install (https://asciidoctor.org/)[Asciidoctor]. Then, run the following command inside the `docs` directory:

```sh
asciidoctor wolf-shaper.adoc -o index.html
```
